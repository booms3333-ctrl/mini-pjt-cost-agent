# SAFEGUARDS · 운영 안전장치 4종

## 배경

[MODEL_FAILOVER.md](MODEL_FAILOVER.md)의 서킷 브레이커는 "Bedrock 모델 하나가 막히는"
한 가지 실패 모드만 다룬다. 실제 코드를 다시 훑어보니 이것과 같은 성격(=한 군데가
막히면 서비스 전체 또는 요청 하나가 무한정 물려버림)의 위험이 4곳 더 있었다 —
`app.py`/`pipeline.py`에 요청 전체를 감싸는 타임아웃이 아예 없었고, MCP 자식
프로세스가 죽어도 재시작 전까지 계속 실패했고, HITL 승인 대기가 아무리 오래
지나도 그대로 재개 가능했고, 서브 에이전트별 도구 호출 한도(`MAX_TOOL_CALLS_PER_AGENT`)만
있어서 Plan-Execute처럼 여러 에이전트가 한 턴에 도는 질문의 LLM 호출 총량에는
상한이 없었다. 이 문서는 그 4곳에 추가한 안전장치를 다룬다.

## 안전장치 1 — 요청 전체 타임아웃

**위치**: `src/pipeline.py`의 `REQUEST_TIMEOUT_SECONDS = 90`, `ask()`/`resume()`
둘 다 `graph.ainvoke(...)`를 `asyncio.wait_for(..., timeout=REQUEST_TIMEOUT_SECONDS)`로
감싼다.

**문제**: 서킷 브레이커·재시도는 "모델 하나가 막혔을 때 다음 모델로 넘어가는" 것만
보장하지, 9개 모델이 전부 느려지거나(예: 계정 전체 스로틀링) MCP 도구 호출 하나가
응답 없이 멈추는 경우에는 요청이 끝없이 걸려 있을 수 있다. HTTP 클라이언트나
사용자 쪽에서 영원히 기다리게 되는 문제.

**동작**: 타임아웃에 걸리면 `TimeoutError`를 잡아 "요청 처리 시간이 N초를 넘어
응답을 완성하지 못했습니다. 질문을 더 구체적으로 나눠서 다시 시도해 주세요."라는
안내를 돌려주고, `qa_history`에 `status="error"`로 남긴다. 진행 중이던 LangGraph
실행 자체는 `asyncio.wait_for`가 태스크를 취소하므로 백그라운드에 남지 않는다.

**한계**: 90초는 이 프로젝트의 실측 정상 범위(질문당 5~25초, Plan-Execute 복합
질문도 대개 30초 이내)에 여유를 곱한 값이지, 이론적으로 유도한 값이 아니다. 정상
질문이 더 무거워지면(에이전트/도구 추가 등) 같이 늘려야 할 수 있다.

**검증**: `REQUEST_TIMEOUT_SECONDS`를 0.01초로 낮추고 실제 질문(실 Bedrock+MCP
경로)을 던져 0.02초 만에 타임아웃 응답으로 끊기는 것을 확인했다.

## 안전장치 2 — MCP 세션·그래프 강제 재기동

**위치**: `src/app.py`의 `_graph_broken`(모듈 전역 플래그), `mark_graph_broken()`,
`_get_graph()`의 재기동 분기, `/query`·`/query/approve` 핸들러의 `try/except`.

**문제**: MCP 서버 3개(aws/gcp/azure cost 서버)는 `open_persistent_mcp_tools()`로
프로세스 수명 동안 세션을 한 번만 열어 재사용한다([MODEL_FAILOVER.md] 성능 문서화
패턴과 같은 이유). 그런데 그중 하나의 자식 프로세스가 죽으면(비정상 종료, 크래시
등) 그 세션을 물고 있는 그래프의 모든 이후 호출이 서버를 재시작하기 전까지
계속 실패한다 — 지금까지는 이 경로에 대한 복구 수단이 전혀 없었다.

**동작**: `/query`·`/query/approve`가 `pipeline.ask()`/`resume()` 호출을
`try/except`로 감싸고, 예외가 새어나오면 (a) 전체 예외를 서버 로그에만 남기고
사용자에게는 내부 정보 없는 일반 오류 메시지만 돌려주며(`guardrails.mask_pii`와
같은 "내부 정보 노출 금지" 원칙), (b) `mark_graph_broken()`으로 전역 플래그를
세운다. 다음 요청이 `_get_graph()`를 부르면 플래그를 보고 기존 `_mcp_stack`을
닫은 뒤 `open_persistent_mcp_tools()`로 MCP 세션 3개 전부와 그래프를 통째로 다시
만든다.

**한계(의도적 설계)**: 죽은 프로바이더 하나만 골라 재연결하는 대신 **셋 다** 다시
만든다 — 그래프 노드들(`_make_agent_node` 등)이 빌드 시점에 특정 도구 함수 참조를
클로저로 들고 있어서, 살아있는 세션의 도구 객체만 남기고 죽은 것만 바꿔 끼우는
게 불가능하다. "거칠지만 확실한" 전체 재기동을 택했다 — 살아있던 세션 2개도
같이 재시작되는 비용(MCP 서버 기동 ~2초×3)은 있지만, 실패가 서버 재시작 전까지
영구화되는 것보다는 낫다.

**검증**: `app._get_graph()`를 실제로 두 번 호출(같은 객체 재사용 확인) →
`mark_graph_broken()` 호출 → 세 번째 호출에서 `_graph`/`_mcp_stack`이 완전히
새 객체로 교체되고 `_graph_broken`이 다시 `False`로 리셋되는 것을 확인 → 정상
상태에서 네 번째 호출은 다시 같은 객체를 재사용하는 것까지 확인(불필요한 재기동
없음).

## 안전장치 3 — HITL 승인 대기 만료(TTL)

**위치**: `src/pipeline.py`의 `APPROVAL_TTL_SECONDS = 3600`, `resume()` 시작부의
만료 검사.

**문제**: `stop_resource` 같은 되돌릴 수 없는 실행 도구는 HITL(사람 승인)
게이트를 거쳐야 한다(`guardrails`/`_execution_approve_node`). 그런데 승인 대기
상태(`interrupt()`로 멈춘 LangGraph 체크포인트)는 시간 제한이 없어서, 사용자가
승인 요청을 받은 지 며칠이 지난 뒤에 "그때 그 승인, 이제 할게요"로 재개해도
그대로 실행됐다 — 그 사이 리소스 상태나 상황이 바뀌었을 수 있는데도.

**동작**: `resume()`이 그래프를 재개하기 **전에** `qa_history.list_history(thread_id,
limit=1)`로 그 스레드의 가장 최근 기록을 보고, 그게 `status == "pending_approval"`이고
`created_at`으로부터 `APPROVAL_TTL_SECONDS`(기본 1시간)가 지났으면 그래프를 아예
부르지 않고 "N분 전에 만들어져 만료됐습니다 — 새 질문으로 다시 요청해 주세요"를
돌려준다(`status: "expired"`). 실제 실행은 절대 일어나지 않는다.

**한계(의도적 설계)**: 만료 검사는 **실제로 멈춰있는 LangGraph 체크포인트를
지우거나 건드리지 않는다** — `qa_history`(별도의 감사 로그 테이블)만 조회해서
"이번엔 재개를 거절한다"는 판단만 한다. `checkpoints.db`의 내부 스키마를 직접
조작하는 건 이 범위 밖의 리스크로 판단해 손대지 않았다. 그 결과 **같은 pending 상태를
TTL이 지나기 전에 다시 `resume()`하면 여전히 정상 승인/거부로 처리된다** — TTL은
"이 시점 이후의 재개 시도를 걸러내는 문"일 뿐, "만료되면 그 승인 자체가 소멸한다"는
보장은 아니다.

**검증**: FakeLLM + 실제 SQLite(`data/costs.db`, `data/history.db`) 경로로
(1) `pipeline.ask()`로 승인 대기 상태를 만들고, (2) `APPROVAL_TTL_SECONDS=0`으로
낮춰 즉시 `resume()` → `status: "expired"` 응답 + 실제 `actions_log`에 실행 기록
없음(0건)을 확인, (3) TTL을 원래 값으로 되돌리고 같은 스레드를 다시 `resume()` →
이번엔 정상적으로 승인·실행되고 `actions_log`에 정확히 1건 남는 것까지 확인 —
만료 검사가 체크포인트 자체를 건드리지 않는다는 설계대로 동작함을 실측했다.

## 안전장치 4 — 턴 단위 LLM 호출 예산

**위치**: `src/agent.py`의 `TURN_LLM_CALL_BUDGET = 20`, `SupervisorState.llm_calls_made`
(`Annotated[int, operator.add]`)/`llm_calls_baseline`, `_supervisor_node`의 예산
검사.

**문제**: 서브 에이전트별 도구 호출 한도(`MAX_TOOL_CALLS_PER_AGENT`)는 있었지만,
Plan-Execute처럼 한 턴에 여러 에이전트(최대 3개)가 순차로 도는 복합 질문에서
**LLM 호출 총량**에는 상한이 없었다 — 각 에이전트가 한도 안에서만 돌아도 에이전트
수만큼 곱해진다. 라우팅이 잘못돼 같은 에이전트가 반복 호출되는 등의 이상 상황을
막을 안전판이 없었다.

**동작**: `_ReactSubState`/`SupervisorState`에 `llm_calls_made`를 `operator.add`
리듀서로 추가해, 서브 에이전트 ReAct 루프의 LLM 호출(`agent_node`)과
`execution_agent` 결정 노드(`_execution_decide_node`) 각각이 호출 1회당 정확히
1을 더한다(`trace`가 이미 쓰던 것과 같은 "그래프 상태 병합" 패턴 —
[MODEL_FAILOVER.md]의 `active_model` 항목 참고). 새 턴이 시작될 때
`llm_calls_baseline`에 그 시점의 누적값을 기록해두고, 같은 턴 안에서 다음
에이전트로 넘어갈 때마다 `(누적 - baseline)`이 `TURN_LLM_CALL_BUDGET`을 넘었는지
검사한다. 넘었으면 남은 `pending` 에이전트를 전부 비우고 "이번 턴에서 처리 가능한
LLM 호출 예산을 다 써서 여기까지만 확인했습니다. 질문을 더 구체적으로 나눠서 다시
물어봐 주세요"를 돌려준다.

**예산 값의 근거**: `route_question`이 한 턴에 최대 3개 에이전트를 디스패치할 수
있고, 에이전트 하나가 `MAX_TOOL_CALLS_PER_AGENT`(도구 호출 한도) 안에서 정상적으로
쓰는 LLM 호출은 대략 4~7회 — 3개 에이전트 × 7회 ≈ 21회가 "정상적인 최악의 경우"다.
20은 이보다 살짝 낮지만, 실측 정상 질문들의 평소 사용량(문항당 3~10회 수준)에는
여유가 있어 평소 동작을 방해하지 않는다.

**한계(의도적 근사)**: 이 카운터는 **그래프 노드 단위 LLM 호출만** 센다 —
`retrieve_docs` 도구 내부의 쿼리 확장/리랭킹 LLM 호출이나 `answer_chain`의 구조화
호출(둘 다 그래프 노드 바깥, LCEL 체인 안에서 일어남)은 포함하지 않는다. 그래서
"진짜 총 LLM 호출 수"가 아니라 "그래프가 직접 통제할 수 있는 호출 수"의 근사치다 —
그래도 폭주를 막는 안전판으로는 충분하다고 판단했다(정확한 총량 계측은 이
프로젝트 범위 밖의 더 큰 리팩터링이 필요).

**검증**: 실제 Plan-Execute 질문("이번 분기 클라우드 비용 20퍼센트 줄이는 계획
세워줘", RAG+이상탐지+실행 에이전트가 같이 도는 질문)을 실 Bedrock+MCP 경로로
(1) 기본 예산(20)에서 던져 끝까지 정상 처리되는 것을 확인, (2)
`TURN_LLM_CALL_BUDGET=2`로 낮춰 같은 질문을 다시 던져 실제로 중간에 예산 초과
메시지로 끊기는 것을 확인.

## 공통 원칙

- 4개 모두 **가진 걸 쓴다**: `qa_history`(안전장치 3), LangGraph의 상태 병합
  메커니즘(안전장치 4), 이미 있던 예외 처리 지점(안전장치 2) — 새 저장소나 새
  프레임워크를 끌어오지 않았다.
- 4개 모두 **정상 경로에는 개입하지 않는다** — 타임아웃은 90초 안에 끝나는
  질문에는 아무 영향이 없고, MCP 재기동은 안 깨졌으면 그대로 기존 그래프를
  재사용하고, TTL은 1시간 안에 재개하면 평소와 똑같고, LLM 호출 예산은 20회
  밑에서는 아무 메시지도 안 붙는다. "안전장치가 있다는 걸 사용자가 평소에는
  전혀 느끼지 못해야 한다"는 기준으로 값을 골랐다.
- 4개 모두 **부분적·근사적 복구**를 택했다 — 정밀한 복구(프로바이더 하나만
  재연결, 정확한 전체 LLM 호출 계측, 체크포인트 직접 조작)는 전부 이
  프로젝트 범위에서는 리스크가 이득보다 크다고 판단해 일부러 하지 않았다. 각
  절의 "한계" 문단이 그 근거다.
- [MODEL_FAILOVER.md]의 서킷 브레이커와 성격은 비슷하지만(둘 다 "막힌 걸
  기억해서 반복 실패를 막는다") 다루는 실패 모드는 다르다 — 서킷 브레이커는
  "모델 하나", 이 문서는 "요청 전체/MCP 세션/승인 대기/턴 전체"다.

## 갱신 시 체크리스트

- `TURN_LLM_CALL_BUDGET`/`REQUEST_TIMEOUT_SECONDS`/`APPROVAL_TTL_SECONDS`를
  바꾸면 이 문서의 근거 문단도 같이 갱신한다.
- 서브 에이전트나 `route_question`의 최대 동시 디스패치 수를 바꾸면
  안전장치 4의 예산 근거를 다시 계산한다.
- `tests/test_regression.py`는 그대로 통과해야 한다 — 4개 안전장치 모두 FakeLLM
  주입 경로(`llm=fake_llm`)의 결정론적 동작을 바꾸지 않는다(안전장치 1/2는 예외
  처리 경로라 정상 흐름에 개입하지 않고, 안전장치 3은 `pipeline.resume()`에서만
  검사하고 `tests/test_regression.py`의 HITL 테스트는 `graph.ainvoke()`를 직접
  불러 이 경로를 타지 않으며, 안전장치 4는 기본값 20이 테스트의 짧은 시나리오보다
  훨씬 커서 걸리지 않는다).
